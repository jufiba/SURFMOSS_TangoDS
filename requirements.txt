# Core Tango framework — required by all device servers
pytango

# Serial communication — required by most active servers
pyserial

# Scientific computing
numpy        # WisselMCA, scripts/, inactive/VSMControlDevice
matplotlib   # scripts/, inactive/VSMControlDevice

# USB HID — required by WisselMCA.
# Must be cython-hidapi (PyPI "hidapi"), which provides hid.device().
# The PyPI package called "hid" is a different project: it exposes hid.Device()
# and would fail with AttributeError here.
# Needs the libhidapi shared library at runtime (Debian: libhidapi-hidraw0).
hidapi

# Qt GUI — only needed for scripts/LEEMgui.py (LEEMmacros.gui())
PySide6

# PID controller — required by PIDController
simple-pid

# USB-TMC instrument communication — inactive/Keithley2100
usbtmc

# Computer vision — inactive/V4L2Camera
opencv-python

# Raspberry Pi GPIO — required by RaspberryButton, RaspberrySwitch, TempSensorDS18B20
# Uncomment on Raspberry Pi targets:
# RPi.GPIO
# w1thermsensor

# MCC 1208LS DAQ — required by MCC1208LS
# Not available on PyPI; install from source:
# https://github.com/wjasper/Linux_Drivers
# usb_1208LS
